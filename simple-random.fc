;; Simple random smart contract

() recv_internal(slice in_msg) impure {
    ;; noop
}

() recv_external(slice in_msg) impure {
    accept_message();
}

int get_random() method_id {
    return random();
}